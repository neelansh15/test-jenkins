pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                // javac 'helu.java'
                // helu_groovy= load 'helu.groovy'
                // helu_groovy.helu()
                java helu
            }
        }
    }
}
